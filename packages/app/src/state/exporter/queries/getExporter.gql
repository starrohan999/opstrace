# Returns exporter configuration for a single entry.
query GetExporter($id: uuid!) {
  exporter_by_pk(id: $id) {
    id
    tenant_id
    name
    type
    credential {
      id
      name
    }
    config
    created_at
    updated_at
  }
}
