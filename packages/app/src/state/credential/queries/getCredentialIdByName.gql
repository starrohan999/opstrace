# Returns credential ID given a user-facing name
query GetCredentialIdByName($tenant_id: uuid!, $name: String!) {
  credential(
    where: {
      tenant_id: {_eq: $tenant_id},
      name: {_eq: $name}
    }
  ) {
    id
  }
}
